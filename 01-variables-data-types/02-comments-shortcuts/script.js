// This double forward slash creates a single line comment

// click on any line you want to comment and hit cmd + / to COMMENT IT OUT.

// to SELECT and HIGHLIGHT, click on and hold shift then arrow key.

// cmd + right/left to go to the end/begninning of a line.

// shift + opt + left/right selects and highlights whole words

// opt + left/right arrow to go to the beginning/end of a word.

// shift + cmd + arrow to highlight from where you clicked to the end or beginning of the line or all the way up or down the document.

// opt + up/down to MOVE A LINE up or down.

// shift + option after clicking on a line to DUPLICATE it.

// after selecting a word, hit cmd + D to select other words like it one by one to then edit at the same time.

// after selecting a word hit cmd + shift + L to select all the othe words like it at the same time and then edit at the same time.

// to PLACE MULTIPLE CURSORS, hold opt and click wherever you want the cursors to be.

// cmd + shift + O to open SEARCH area to find a file.

// cmd + opt + F to search for a word within the document.

// cmd + shift + F to search for a word in ALL documents.

// ctrl + ~ to open the terminal.

/* This is a multi line comment
console.log(100);

console.log("hello world");

console.log(20, "hello", true);
*/

const x = 200;

console.log(x);

console.error("Alert");

console.warn("Warning");

console.table({ name: "yaser", email: "test@example.com" });

console.group("sample group");
console.log(x);
console.error("Alert");
console.warn("Warning");
console.groupEnd();

const styles = "padding: 10px; background-color: grey; color: white;";
console.log("%cHello World", styles);
