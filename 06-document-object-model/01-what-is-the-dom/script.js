// What is the Document Object Model?

// It's a programming interface for web/HTML elements.
// It's a structure that we can interact with using JavaScript.
// It includes tags, attributes, text nodes etc.
// It's represented as a tree structure

console.log(window);
console.log(document);
console.log(document.body.innerHTML);
console.log(document.body.innerText);
console.log(document.links[0]);
